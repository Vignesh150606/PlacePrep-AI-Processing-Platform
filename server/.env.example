# Copy this file to `.env` (same folder) and fill in real values.
# `.env` is gitignored -- never commit real secrets.

# --- App ---
ENVIRONMENT=development
CORS_ORIGINS=http://localhost:5173
# Optional. Exact-match origins above go stale fast on Vercel, which mints a
# new preview URL (<project>-<hash>.vercel.app) on every deploy. Set a regex
# here instead so any preview deployment of your project is trusted without
# editing this value again -- see config.py's CORS_ORIGIN_REGEX docstring.
# CORS_ORIGIN_REGEX=^https://your-project(-[a-zA-Z0-9]+)*\.vercel\.app$

# --- Supabase ---
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=

# --- Storage ---
PDF_STORAGE_BUCKET=pdfs
MAX_PDF_SIZE_BYTES=26214400
MAX_IMAGE_SIZE_BYTES=15728640

# --- AI provider ---
AI_PROVIDER=gemini
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
AI_CONFIDENCE_THRESHOLD=0.7
DUPLICATE_SIMILARITY_THRESHOLD=0.87
MAX_EXTRACTION_ATTEMPTS=3

# --- Chunking ---
CHUNK_MAX_CHARS=12000
CHUNK_OVERLAP_CHARS=400

# --- OCR fallback ---
OCR_ENABLED=true
OCR_MIN_CHARS_PER_PAGE=40
OCR_DPI=200

# --- Rate limiting (Phase 6) ---
# In-memory limiter -- fine for a single-process dev/small deploy. On a
# multi-instance deploy (multiple Render/Railway dynos), each instance
# tracks its own counters, so effective limits multiply by instance count.
# Swap to a Redis storage_uri (see app/core/rate_limit.py) before scaling
# horizontally.
RATE_LIMIT_ENABLED=true
RATE_LIMIT_UPLOAD=10/minute
RATE_LIMIT_QUIZ_SUBMIT=30/minute
RATE_LIMIT_DEFAULT=120/minute
